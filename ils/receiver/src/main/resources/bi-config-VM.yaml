net:
  bindIp: "127.0.0.1"
  port: 3307
  ssl:
    mode: "requireSSL"
    PEMKeyFile: "C:/ContainOpenSource/OpenSourceJava/ils/receiver/src/main/resources/mongosqld.pem"

security:
  enabled: true # Change this from false to true

schema:
  sample:
    namespaces: ["ilstools.*"]