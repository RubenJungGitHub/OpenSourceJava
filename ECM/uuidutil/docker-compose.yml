version: '3.8'

services:
  uuidutil:
    build:
      context: ..                     # root of your repo
      dockerfile: uuidutil/Dockerfile
    ports:
      - "5002:8080"                    # map container 8080 to host 5002